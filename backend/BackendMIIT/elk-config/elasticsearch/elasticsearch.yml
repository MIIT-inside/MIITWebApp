cluster:
  name: "elasticsearch"
network:
  host: 0.0.0.0

action:
  auto_create_index: ".security*,.monitoring*,.watches,.triggered_watches,.watcher-history*,.ml*"

xpack:
  security:
    enabled: true
  license:
    self_generated:
      type: basic
