server:
  name: kibana
  host: 0.0.0.0
  url: "http:localhost:5601"

elasticsearch:
  hosts: [ "http://elasticsearch:9200" ]
  username: elastic
  password: elastic

monitoring:
  ui:
    container:
      elasticsearch:
        enabled: true